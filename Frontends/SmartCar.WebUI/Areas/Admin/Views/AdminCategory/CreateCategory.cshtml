@model CreateCategoryDto

@{
    ViewData["Title"] = "CreateCategory";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Thêm danh mục bài viết mới</h4>
                        <p class="card-subtitle mb-4">Bạn có thể nhập thông tin danh mục trong biểu mẫu bên dưới.</p>
                        <form method="post">@Html.AntiForgeryToken()
                            <div class="form-group">
                                <label for="exampleInputName">Tên danh mục</label>
                                <input type="text" class="form-control" id="exampleInputName" asp-for="Name" placeholder="Nhập tên danh mục">
                            </div>
                            <button type="submit" class="btn btn-primary waves-effect waves-light">Lưu danh mục</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

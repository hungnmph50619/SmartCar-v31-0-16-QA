@model List<ResultAllBlogsWithAuthorDto>
@{
    int count = 0;
}

<div class="col-xl-6">
    <div class="card">
        <div class="card-body">

            <h4 class="card-title">Bài viết mới nhất</h4>
            <p class="card-subtitle mb-4 font-size-13">
                5 bài viết mới nhất của tác giả
            </p>

            <div class="table-responsive">
                <table class="table table-borderless table-hover table-centered table-nowrap mb-0">
                    <tr>
                        <th>Tiêu đề bài viết</th>
                        <th>Tác giả</th>
                        <th>Mở bài viết</th>
                    </tr>
                    <tbody>
                        @foreach(var item in Model)
                        {
                            <tr>
                            <td><h5 class="font-size-15 mb-1 font-weight-normal">@item.title</h5></td>
                            <td><h5 class="font-size-17 mb-1 font-weight-normal">@item.authorName</h5></td>
                            <td><h5 class="font-size-15 mb-1 font-weight-normal"><a href="/Blog/BlogDetail/@item.blogID" class="btn btn-primary">Mở bài viết</a></h5>
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div> <!-- end card-body-->
    </div> <!-- end card-->
</div>